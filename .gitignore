# Runtime state — never commit
sessions/
memory/history.jsonl
memory/vectors.db
cron/jobs.json
docs/_site/
docs/.jekyll-cache/
docs/Gemfile.lock
.env
# Publisher working clone — never commit
patria-site/
