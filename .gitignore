# Runtime state — never commit
sessions/
memory/history.jsonl
memory/vectors.db
memory/pipeline/
memory/media/
memory/research/
memory/drafts/
cron/jobs.json

# Nanobot internal cache — never commit
.nanobot/

# Python cache
**/__pycache__/
*.pyc

# Temp files
temp_*.py

# Contains secrets (Discord token) — see config.json.example
config.json
docs/_site/
docs/.jekyll-cache/
docs/Gemfile.lock
.env
# Publisher working clone — never commit
patria-site/
